function sumSubs(numOne, numTwo, numThree) {
    // let sumOneTwo = (a, b) => (a + b)
    // let substractThree = (a, b) => (a - b)
    // let firstResult = sumOneTwo(numOne, numTwo)
    // let finalResult = substractThree(firstResult, numThree)
    // return finalResult
    return (numOne+numTwo) - numThree
}

console.log(sumSubs(1,
    17,
    30))